" Make it so that vim doesn't ask to save a file when it goes hidden
set hidden

" Map <Leader>b to display a list of buffers and then wait for input on which to switch to
nnoremap <Leader>b :buffers<CR>:buffer<Space>

" Change the leader key
" let mapleader = ","

