#ifndef FUNCTIONS_H
#define FUNCTIONS_H
#include <stddef.h>

//int add_numbers(int a, int b);
//void reverse_string(char* str);

int add_array_elements(int *a, size_t size);

#endif // FUNCTIONS_H